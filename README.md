# Colab-Neural-Network-Project-for-material-design
Predicting molecular formation enthalpy from atomic structures using SOAP descriptors and PyTorch neural networks.

Recommend opening this up in colab to avoid compatibility and version issues. Warning, the code does take a while to run.
Early in the code it will ask to upload some files to work, those would be "test.json" and "train.json".
